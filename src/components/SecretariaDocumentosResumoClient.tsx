"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { listarDocumentosSecretariaAction } from "@/app/secretaria/documentos/actions";
import { lojaAtivaId } from "@/lib/loja-ativa";
export function SecretariaDocumentosResumoClient(){const[dados,setDados]=useState({revisao:0,aprovacao:0,aprovados:0});useEffect(()=>{const lojaId=lojaAtivaId();if(!lojaId)return;void Promise.all([listarDocumentosSecretariaAction({lojaId,categoria:"Balaústre"}),listarDocumentosSecretariaAction({lojaId,categoria:"Ata Administrativa"})]).then(respostas=>{const itens=respostas.flatMap(r=>r.ok?r.data:[]);setDados({revisao:itens.filter(i=>i.status==="Em revisão").length,aprovacao:itens.filter(i=>i.status==="Aguardando aprovação").length,aprovados:itens.filter(i=>i.status==="Aprovado").length});});},[]);return <section className="rounded-3xl border border-amber-400/20 bg-gradient-to-br from-amber-400/[.08] to-white/[.03] p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-amber-300">Secretaria profissional</p><h2 className="mt-2 text-xl font-bold">Balaústres e Atas</h2><p className="mt-1 text-sm text-slate-400">Tramitação, aprovação, bloqueio e PDF institucional.</p></div><Link href="/secretaria/balaustres" className="rounded-xl bg-amber-400 px-5 py-3 text-center font-bold text-black">Abrir documentos</Link></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[["Em revisão",dados.revisao],["Aguardando aprovação",dados.aprovacao],["Aprovados",dados.aprovados]].map(([rotulo,valor])=><div key={String(rotulo)} className="rounded-xl border border-white/10 bg-black/20 p-4"><p className="text-xs text-slate-500">{rotulo}</p><p className="mt-1 text-2xl font-black text-white">{valor}</p></div>)}</div></section>}
